<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.dao.UserDao">

<insert id="insert">
	insert into 
		users (userNum, userID, userPW, userName, userGender, userBirth, userPhone, userEmail, enabled)
	values (user_counter.nextval, #{userID}, #{userPW}, #{userName}, #{userGender}, #{userBirth}, #{userPhone}, #{userEmail}, #{enabled})
</insert>

<select id="selectAll" resultType="com.spring.dto.UserDto">
select * from users
</select>

<select id="selectName" resultType="com.spring.dto.UserDto">
select * from users where userID=#{userID}
</select>

<update id="update">
update users set 
	userPW=#{userPW}, userName=#{userName}, userGender=#{userGender},
	userBirth=#{userBirth}, userPhone=#{userPhone}, userEmail=#{userEmail}, enabled=#{enabled} where userID=#{userID}
</update>

<delete id="delete">
delete users where userID=#{userID}
</delete>
</mapper>